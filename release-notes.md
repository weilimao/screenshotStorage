1. 优化剪贴板监控性能：采用特征指纹与 Bitmap 比对，避免重复执行 PowerShell 与 PNG 压缩，消除待机 CPU 飙升与内存暴涨。
2. 深度释放后台内存：将主窗口关闭行为从“后台隐藏”改为“直接销毁”，在关闭主面板时直接杀死其 Chromium 渲染进程，释放 80MB+ 内存底噪。
3. 静默启动懒加载：开机自启/静默启动时不再默认加载主窗口，仅按需实例化，大幅降低启动阶段的内存开销。
4. 限制 V8 引擎堆大小：为主进程注入 --max-old-space-size=128 与 --process-per-site 参数，引导系统进行更积极的垃圾回收。
